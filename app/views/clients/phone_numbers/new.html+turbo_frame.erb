<%= turbo_frame_tag @phone_number do %>
  <%= form_with model: @phone_number, url: client_phone_numbers_path(@phone_number.client) do |form| %>
    <div class="grid grid-cols-3">
      <div>
        <%= form.text_field :number_type, class: "border-none focus:outline-none focus:ring-transparent focus:text-green-500" %>
      </div>
      <div>
        <%= form.text_field :number, class: "border-none focus:outline-none focus:ring-transparent focus:text-green-500" %>
      </div>
      <div class="text-right flex gap-4 justify-between">
        <%= form.submit "Save", class: "text-sm border bg-green-100 text-green-700 border border-green-600 rounded py-2 px-3" %>
      </div>
    </div>
  <% end %>
<% end %>