<%= render partial: 'layouts/breadcrumbs', locals: { breadcrumbs: @breadcrumbs } %>

<h1 class="font-bold text-2xl mb-4">Identification</h1>
<div class="grid grid-cols-5 gap-4">
  <div class="col-span-4">
    <%= turbo_frame_tag 'new_identification' do %>
      <%= link_to 'Add Identification', new_client_identification_path(@client), class: "bg-white border border-slate-300 rounded py-2 px-4 mb-4 text-sm block" %>
    <% end  %>
    <%= turbo_frame_tag 'identifications' do %>
      <%= render @identifications %>
    <% end %>
  </div>
  <%= render partial: "clients/clients/proposal_status" %>
</div>